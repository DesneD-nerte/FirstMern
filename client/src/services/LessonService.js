import { RRule, RRuleSet, rrulestr } from 'rrule'

class LessonService {
    // separateLessons(appointmentData) {
    //     const {recurrenceRule} = appointmentData;

    //     const rule = rrulestr(recurrenceRule);
         
    //     const occurences = rule.all();
    //     occurences.forEach(date => {
    //         const year = date.getFullYear();
    //         const month = date.getMonth();
    //         const date = date.getDate();
    //         // const hours appointmentData
    //         // const newCurrentLesson = {...appointmentData, startDate: new Date(year, month, date,)}//year:, month:, date?:, hours?:, minutes?:, seconds?:
    //     });
    // }
}

export default new LessonService();